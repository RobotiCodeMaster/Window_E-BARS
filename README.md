# Window_E-BARS
This firmware runs the Window E-BARS system by detecting hazardous temperature conditions and automatically releasing a window guard using a servo. It activates buzzer, RGB, and display alerts, supports manual override, logs events to EEPROM, prints every action into Serial Monitor, and operates with non-blocking timing and configurable controls.
